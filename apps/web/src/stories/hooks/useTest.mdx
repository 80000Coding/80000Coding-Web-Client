import { Canvas, Meta } from '@storybook/blocks'

<Meta title='Hooks/useTest' />

# useTest

storybook에 hooks 사용법을 작성하는 예제입니다.

### How to use

```tsx
const Component = () => {
  const { test } = useTest()
  return <div>{test}</div>
}
```
